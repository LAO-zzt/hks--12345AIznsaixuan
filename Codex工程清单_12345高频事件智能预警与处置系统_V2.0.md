# Codex 工程开发清单 — 12345 高频事件智能预警与处置系统（V2.0 · Agent 对齐即施工版）

> 版本定位：本版在原“12345 多频工单识别系统”基础上升级，不推翻原有技术路线。核心从“发现重复工单”升级为“发现高频事件 → 判断风险 → 形成管理决策结果 → 可选自动流转”。
>
> **本文件既是产品方案，也是 Agent 的施工契约。** Agent 读取本文后，必须先完成一次“思路对齐”，再直接进入工程实施；对齐步骤不要求重新询问已经明确的需求。
>
> **核心原则：先保 Demo 跑通，再增强算法；先保证业务闭环，再增加技术炫技。**

---

## 零、Agent 开工协议（必须先执行）

### 0.1 Agent 首次看到本文件后的固定动作

Agent 不得直接开始大规模改代码，也不得自行重新定义题目。第一步必须向用户用简洁结构复述以下内容：

1. **我要解决的问题**：不是单纯找重复工单，而是从 12345 工单中发现正在形成的高频事件，并输出风险与处置依据。
2. **核心链路**：工单输入 → 清洗 → 标准化 → 主体/事件提取 → 事件聚类 → 多频识别 → 趋势/区域画像 → 风险判断 → 处置建议 → 展示/导出/推送。
3. **技术保底**：默认 TF-IDF + 余弦距离 + DBSCAN，完全离线；Embedding 只做可选增强，运行时绝不下载模型。
4. **业务闭环**：最终页面不能只展示“多频=是/否”，必须让用户看到“发生了什么、影响多大、是否在升温、应该优先关注什么”。
5. **工程边界**：E01 独立使用 Streamlit，不强套 FastAPI；可复用飞书推送逻辑，但不引入与本题无关的 RAG、话术生成等重型模块。
6. **失败兜底**：算法聚类不好时必须回退到可解释的规则分组；任何单点失败都不能让 Demo 崩溃。

### 0.2 对齐输出格式

Agent 第一次响应必须明确写出：

```text
【E01 架构对齐】
业务目标：发现高频事件，并辅助判断风险与处置优先级
技术路线：TF-IDF → 余弦距离 → DBSCAN（主） / Embedding（可选）
业务增强：事件画像 + 时间趋势 + 区域分布 + 风险等级 + 处置建议
前端：Streamlit 独立 Demo
输出：结果表 + 高频事件看板 + 风险看板 + CSV/Excel + 可选飞书
Fallback：规则分组兜底，确保现场必出结果
执行策略：先跑通完整链路，再逐项增强
```

**对齐不是阻塞步骤。** 在上述内容与本文一致的前提下，Agent 直接开始执行；不得为了已经写明的事项重复询问用户。

### 0.3 冲突处理原则

若仓库、旧文档、旧代码与本文件冲突，优先级如下：

1. 用户当前明确指令
2. 本文件 V2.0
3. 当前项目真实代码结构
4. 历史工程清单/旧架构

不得因为旧代码存在某个模块，就强行保留与 V2.0 目标冲突的模块。

---

## 一、项目概述

### 1.1 命题背景

顺德区 12345 热线每天收到大量市民诉求。若不同市民围绕同一事件、同一主体、同一区域持续提交诉求，则可能形成需要重点关注的高频问题。目前主要依赖人工记录、关键词检索和经验判断，识别效率与及时性存在提升空间。

### 1.2 V2.0 核心目标

通过 AI/NLP 方法对工单进行批量分析，完成：

**识别高频事件 → 描述事件画像 → 判断风险变化 → 形成处置建议 → 输出管理结果。**

系统最终不是单纯回答：

> “哪些工单重复？”

而是回答：

> “最近正在形成哪些高频问题？它们发生在哪里、涉及谁、增长是否明显、哪些应该优先处理？”

### 1.3 评价标准

评价重点：

- ☑ 完成度
- ☑ 落地潜力
- ☑ 业务价值表达
- ☑ AI/NLP 应用合理性
- ☑ Demo 可运行性

保持技术克制，不为了“AI 感”而强行调用大模型。

### 1.4 最低成果

Demo 至少必须实现：

1. 批量上传 CSV 工单；
2. 自动完成识别流水线；
3. 识别多频/高频事件；
4. 对高频事件生成事件画像；
5. 给出风险等级或关注等级；
6. 给出处置建议；
7. 用看板和表格直观展示；
8. 支持 CSV/Excel 下载；
9. 默认离线可运行，不依赖运行时网络。

---

## 二、总体业务架构

### 2.1 核心业务链路

```text
用户上传 CSV
    ↓
loader.py
数据加载 / 字段识别 / 清洗 / 去重
    ↓
normalizer.py
文本标准化 / 同义词归一 / 噪声清洗
    ↓
entity_extractor.py
主体 + 事件 + 区域提取
    ↓
cluster.py
语义向量化 → 相似度 → DBSCAN 聚类
    ↓
classifier.py
多频事件识别 / 频次统计
    ↓
event_profiler.py
事件画像：主体 / 区域 / 时间 / 样例 / 频次
    ↓
risk_analyzer.py
趋势判断 / 风险等级 / 关注优先级
    ↓
action_advisor.py
建议关注部门 / 建议动作 / 是否持续监控
    ↓
exporter.py + main.py
看板 / 表格 / CSV / Excel / 可选飞书
```

### 2.2 从“工单聚类”升级为“事件管理对象”

一个有效聚类最终必须被转换成一个“事件对象”，至少包含：

```text
Event
├── event_id
├── event_subject
├── event_type
├── area
├── frequency
├── first_seen
├── last_seen
├── trend
├── risk_level
├── priority_score
├── sample_orders
└── action_advice
```

### 2.3 业务判断原则

系统不得把“频次高”直接等价为“高风险”。

默认判断逻辑：

```text
风险等级 = 频次 + 增长趋势 + 空间集中度 + 事件敏感度 的综合规则结果
```

当前阶段不要求复杂机器学习风险模型，使用可解释规则即可。

---

## 三、工程约束（Codex 必须遵守）

1. **语言**：Python 3.10+
2. **框架**：Streamlit（Demo 界面）
3. **依赖**：requirements.txt，默认路径尽量少依赖
4. **数据格式**：输入 CSV；输出 CSV/Excel
5. **默认向量化路径**：scikit-learn TF-IDF + jieba 中文分词
6. **聚类方法**：余弦距离 + DBSCAN
7. **Embedding**：仅作为可选增强，不得运行时联网下载
8. **离线可用**：默认路径完全离线
9. **模块化**：每个核心功能独立文件
10. **关键函数**：必须有中文注释
11. **容错优先**：任何非关键模块失败不得阻断主流程
12. **Demo 优先**：现场操作步骤尽量控制在“上传 → 一键分析 → 看板展示”
13. **不自行增加重型功能**：不得无授权加入数据库、微服务、消息队列、复杂 Agent 编排、RAG、大模型 API 等与当前 Demo 非必要的能力
14. **数据真实到位后优先适配真实字段与业务口径，不要重写整体架构**

---

## 四、文件结构（Codex 按此生成）

```text
multi_freq_order/
├── main.py                      # Streamlit 入口
├── requirements.txt             # 依赖清单
├── config.py                    # 全局配置
├── data/
│   ├── input/
│   │   └── sample.csv           # 样例数据
│   ├── output/
│   └── dicts/
│       ├── synonyms.csv         # 同义词规范化
│       ├── subjects.txt         # 主体关键词
│       ├── events.txt           # 事件关键词
│       └── sensitive_events.txt # 可选：敏感事件词
├── modules/
│   ├── __init__.py
│   ├── loader.py                # 数据加载与清洗
│   ├── normalizer.py            # 内容标准化
│   ├── entity_extractor.py      # 主体/事件/区域提取
│   ├── cluster.py               # 聚类
│   ├── classifier.py            # 多频识别
│   ├── event_profiler.py        # 事件画像
│   ├── risk_analyzer.py         # 风险/趋势分析
│   ├── action_advisor.py        # 处置建议
│   ├── exporter.py              # 导出
│   └── feishu_pusher.py         # 可选飞书推送
└── utils/
    ├── __init__.py
    └── helpers.py               # 工具函数
```

---

## 五、功能模块详细清单

### 模块 1：数据加载与预处理（loader.py）

**功能要求：**

- 1.1 读取 CSV 工单数据
- 1.2 自动识别字段列名
- 1.3 至少兼容：工单编号、诉求内容、涉及主体、事发区域、提交时间
- 1.4 支持中文/英文列名映射
- 1.5 找不到标准字段时，进行模糊匹配或位置兜底
- 1.6 去除空行
- 1.7 按工单编号去重
- 1.8 时间字段统一为可计算格式
- 1.9 输出清洗后的 DataFrame

**输出：**

标准字段：

```text
order_id
content
subject
area
submit_time
```

---

### 模块 2：工单内容标准化（normalizer.py）

**功能要求：**

- 2.1 去除特殊符号和无意义噪声
- 2.2 统一全半角
- 2.3 同义词归一
- 2.4 使用本地 `synonyms.csv`
- 2.5 不调用大模型和付费 API
- 2.6 保留原始诉求内容，不覆盖原始字段
- 2.7 输出 `normalized_content`

示例：

```text
下水道堵了 → 排水堵塞
下水道堵塞 → 排水堵塞
工地半夜施工 → 夜间施工噪音
烧烤店吵 → 餐饮噪音扰民
```

---

### 模块 3：实体与事件识别（entity_extractor.py）

**功能要求：**

从 `content + subject + area` 中提取：

- `extracted_subject`：核心主体
- `extracted_event`：核心事件
- `extracted_area`：核心区域

规则优先级：

1. 已有结构化字段
2. 本地词典
3. 后缀规则
4. 正则表达式
5. 无法判断时保留为空，不得伪造

主体示例：

```text
XX小区
XX花园
XX路
XX市场
XX学校
XX医院
XX工业园
```

事件示例：

```text
噪音扰民
夜间施工噪音
违建
占道经营
环境卫生
排水堵塞
油烟污染
停车纠纷
设施损坏
物业管理
```

---

### 模块 4：聚类（cluster.py）【核心识别引擎】

**功能要求：**

- 4.1 拼接：`normalized_content + extracted_subject + extracted_event + extracted_area`
- 4.2 默认使用 TF-IDF
- 4.3 jieba 中文分词
- 4.4 `ngram_range=(1,2)`
- 4.5 使用余弦距离
- 4.6 使用 DBSCAN 聚类
- 4.7 不预设类别数量
- 4.8 噪声点 `-1` 保留

可选 Embedding 路径：

- 只允许从本地 `MODEL_DIR` 加载
- 不得联网下载
- 模型缺失自动回退 TF-IDF

### 4.9 规则兜底

若：

- 有效聚类数量异常
- 聚类结果过少
- 聚类结果明显碎片化
- 参数无法得到可解释结果

则回退到：

```text
extracted_subject + extracted_event + extracted_area
```

的规则分组。

**目标不是强行得到“数学上最优”的聚类，而是确保业务上稳定地产出可解释事件。**

---

### 模块 5：多频事件识别（classifier.py）

**功能要求：**

- 5.1 统计每个 cluster 的工单数量
- 5.2 `cluster_id = -1` 不参与多频判断
- 5.3 默认阈值 `FREQ_THRESHOLD = 3`
- 5.4 支持 UI 调整
- 5.5 输出：

```text
is_multi_freq
cluster_size
cluster_subject
cluster_event
cluster_area
```

**重要：**

此模块负责“是不是多频”，不负责最终“是不是高风险”。

---

### 模块 6：事件画像（event_profiler.py）【V2.0 新增】

对每一个识别出的多频事件生成管理视角的事件画像。

**至少计算：**

- `event_id`
- `event_subject`
- `event_type`
- `area`
- `frequency`
- `first_seen`
- `last_seen`
- `sample_orders`
- `area_count`
- `unique_submitters`（若真实数据提供）

### 6.1 时间窗口统计

至少支持：

- 最近 24 小时
- 最近 7 天
- 数据集整体

若样例数据时间跨度不足，则自动退化为整体统计，不得报错。

### 6.2 趋势计算

优先比较：

```text
近期频次 vs 前期频次
```

输出：

```text
trend = 上升 / 平稳 / 下降 / 无法判断
```

没有足够时间数据时输出“无法判断”，不要编造趋势。

---

### 模块 7：风险与优先级判断（risk_analyzer.py）【V2.0 新增】

**目标：**

把“多频”进一步转化为“值得多大程度关注”。

不使用复杂训练模型，默认使用可解释规则评分。

### 7.1 风险因子

建议至少考虑：

```text
频次
增长趋势
区域集中度
事件敏感度
```

### 7.2 默认评分示意

```text
priority_score =
    frequency_score
  + trend_score
  + area_score
  + sensitivity_score
```

评分权重集中在 `config.py`，不得散落在业务代码中。

### 7.3 风险等级

输出：

```text
高关注
中关注
一般
```

注意：

- 风险等级是“管理优先级”，不是安全事故定性。
- 无充分依据时必须保守。
- 所有等级都必须能在 UI 中看到形成原因。

### 7.4 解释字段

必须输出：

```text
risk_reason
```

示例：

```text
近7天内出现8次，近3天频次明显上升，集中发生于同一区域。
```

---

### 模块 8：处置建议（action_advisor.py）【V2.0 新增】

**目标：**

不是替管理部门做决定，而是提供“下一步应该关注什么”的辅助建议。

### 8.1 建议类型

根据事件类型和风险等级，输出：

```text
建议关注部门
建议动作
是否持续监控
是否进入重点事件列表
```

### 8.2 规则示例

```text
噪音扰民 → 建议关注：城管/相关执法部门
排水堵塞 → 建议关注：住建/市政相关部门
物业管理 → 建议关注：住建/街道相关部门
环境卫生 → 建议关注：环卫/属地管理部门
```

**注意：** 以上只是规则种子，真实主办方业务口径到位后再调整。

### 8.3 建议不能伪造

如果无法从事件类型映射到部门，输出：

```text
需人工研判
```

而不是猜测一个部门。

---

### 模块 9：结果导出（exporter.py）

输出 CSV + Excel。

结果至少包含：

```text
工单编号
原始诉求
涉及主体
事发区域
提交时间
标准化内容
核心主体
核心事件
聚类ID
聚类频次
是否多频
事件趋势
风险等级
优先级分数
风险原因
建议关注部门
建议动作
是否持续监控
```

排序优先级：

1. 高关注
2. 中关注
3. 一般
4. 频次降序
5. 提交时间倒序

---

### 模块 10：Streamlit 演示界面（main.py）

页面必须围绕“管理者看到什么”设计，而不是围绕“程序跑了什么”设计。

### 10.1 页面结构

```text
顶部 KPI
├── 工单总量
├── 多频事件数
├── 高关注事件数
└── 最大频次事件

↓

高频事件看板
├── 事件名称
├── 区域
├── 频次
├── 趋势
├── 风险等级
└── 优先级

↓

事件详情
├── 事件画像
├── 时间趋势
├── 区域分布
├── 典型工单
├── 风险原因
└── 处置建议

↓

完整工单表

↓

下载 / 飞书推送
```

### 10.2 上传操作

支持：

- CSV 上传
- 一键分析
- 参数设置

### 10.3 侧边栏参数

至少包含：

```text
FREQ_THRESHOLD
CLUSTER_EPS
CLUSTER_MIN_SAMPLES
USE_EMBEDDING
```

风险评分参数允许开发阶段调试，但默认隐藏复杂参数，避免路演现场操作混乱。

### 10.4 结果展示原则

不能只给一个大表格。

必须让评委在 10 秒内看懂：

> “系统今天发现了几个值得关注的问题。”

然后在点击某个事件后看到：

> “为什么它值得关注？”

最后看到：

> “建议下一步做什么？”

---

## 六、可视化增强项（建议实现，但不得阻塞主流程）

### 6.1 区域分布

利用 `area` 统计高频事件分布。

### 6.2 时间趋势

按日/小时展示事件频次变化。

### 6.3 人工 vs AI

可用简单静态对比表达：

```text
传统关键词检索：需要人工逐条搜索
AI 聚类识别：批量识别 + 自动归并
```

若没有真实人工基准数据，不得伪造具体“提升 90%”之类数字。

### 6.4 视觉原则

看板优先展示：

```text
数量
趋势
风险
区域
建议
```

不要把页面做成算法调参面板。

---

## 七、可选飞书流转（落地潜力加分项）

### 7.1 推送内容

推送 Top-N 高关注事件：

```text
事件名称
区域
频次
趋势
风险等级
风险原因
建议动作
样例工单号
```

### 7.2 配置策略

- `FEISHU_WEBHOOK = ""` 时跳过
- 网络失败只警告，不阻塞主流程
- 不把 Webhook 写死在代码中
- 不提交真实密钥

### 7.3 业务表达

飞书不是为了“证明系统能发消息”，而是为了证明：

```text
识别结果 → 自动同步 → 进入管理人员工作流
```

---

## 八、数据字段规范

### 输入字段

至少兼容：

| 字段名 | 说明 | 示例 |
|---|---|---|
| order_id | 工单编号 | GZ20260814001 |
| content | 诉求内容 | XX小区楼下烧烤店深夜噪音扰民 |
| subject | 涉及主体 | XX小区楼下烧烤店 |
| area | 事发区域 | 大良街道 |
| submit_time | 提交时间 | 2026-08-14 22:30 |

### 输出字段

除原有识别字段外，V2.0 必须增加：

| 字段名 | 说明 |
|---|---|
| event_id | 事件编号 |
| frequency | 事件频次 |
| trend | 趋势 |
| first_seen | 首次出现时间 |
| last_seen | 最近出现时间 |
| risk_level | 风险/关注等级 |
| priority_score | 优先级分数 |
| risk_reason | 风险原因 |
| action_department | 建议关注部门 |
| action_advice | 建议动作 |
| monitor_required | 是否持续监控 |

---

## 九、配置文件（config.py）

建议包含：

```python
FREQ_THRESHOLD = 3

INPUT_DIR = "data/input/"
OUTPUT_DIR = "data/output/"
DICT_DIR = "data/dicts/"
MODEL_DIR = "models/"

USE_EMBEDDING = False
EMBEDDING_MODEL = "paraphrase-multilingual-MiniLM-L12-v2"

CLUSTER_EPS = 0.5
CLUSTER_MIN_SAMPLES = 2
FALLBACK_RULE_GROUP = True

TREND_RECENT_DAYS = 3
TREND_BASELINE_DAYS = 7

RISK_WEIGHT_FREQUENCY = 0.35
RISK_WEIGHT_TREND = 0.30
RISK_WEIGHT_AREA = 0.15
RISK_WEIGHT_SENSITIVITY = 0.20

FEISHU_WEBHOOK = ""
```

所有可调参数集中管理。

---

## 十、完整处理流程（Codex 严格按照此顺序实现）

```text
用户上传 CSV
    ↓
loader.py
加载 / 清洗 / 去重 / 字段统一
    ↓
normalizer.py
文本标准化
    ↓
entity_extractor.py
主体 + 事件 + 区域提取
    ↓
cluster.py
TF-IDF / Embedding → 余弦距离 → DBSCAN
    ↓
classifier.py
识别多频事件
    ↓
event_profiler.py
生成事件画像
    ↓
risk_analyzer.py
趋势 + 风险等级 + 优先级
    ↓
action_advisor.py
建议关注部门 + 建议动作 + 持续监控
    ↓
exporter.py
CSV / Excel
    ↓
main.py
管理看板 + 详情 + 下载
    ↓
可选 feishu_pusher.py
同步管理工作流
```

---

## 十一、Fallback 与现场容错规则

### 11.1 文件异常

上传非 CSV、文件为空、字段缺失：

- 页面给出明确提示
- 不抛出不可读错误
- 保留 Demo 页面

### 11.2 聚类异常

聚类参数不合理或聚类质量差：

```text
DBSCAN
 ↓
规则分组 fallback
 ↓
仍可生成多频结果
```

### 11.3 时间异常

时间解析失败：

- 仍允许完成主体/事件/频次识别
- 趋势字段显示“无法判断”
- 风险评分不因时间字段缺失而崩溃

### 11.4 Embedding 异常

模型缺失：

```text
提示：本地模型不可用，已自动切换 TF-IDF
```

不得报错退出。

### 11.5 飞书异常

推送失败：

- 告警
- 不影响结果展示
- 不影响文件下载

### 11.6 风险计算异常

任何风险评分模块异常：

- 多频识别结果仍然有效
- 风险等级降级为“需人工研判”
- 页面不得崩溃

---

## 十二、样例数据预案

主办方真实数据未到位前，先用 `sample.csv` 跑完整链路。

样例必须刻意包含：

1. 同一主体、多次投诉
2. 同一事件、不同描述
3. 同一区域、不同主体
4. 不相关噪声工单
5. 时间跨度数据
6. 至少 1 个明显上升趋势事件
7. 至少 1 个稳定事件
8. 至少 1 个无法聚类的噪声点

目的不是制造“漂亮数据”，而是验证整个系统的业务链路。

---

## 十三、Demo 验收标准（必须全部通过）

### A. 能跑

- [ ] `pip install -r requirements.txt` 后默认环境可启动
- [ ] 默认路径无需联网下载模型
- [ ] Streamlit 能正常启动

### B. 能识别

- [ ] CSV 可以上传
- [ ] 中文字段可以识别
- [ ] 工单可以清洗
- [ ] 聚类可以完成
- [ ] 多频事件可以识别

### C. 能解释

- [ ] 每个高频事件都有主体
- [ ] 有事件类型
- [ ] 有区域
- [ ] 有频次
- [ ] 有趋势或明确“无法判断”
- [ ] 有风险原因

### D. 能决策

- [ ] 有风险/关注等级
- [ ] 有优先级
- [ ] 有建议关注部门或“需人工研判”
- [ ] 有建议动作

### E. 能演示

- [ ] 首页 10 秒内看懂关键结果
- [ ] 点击事件可以查看详情
- [ ] 可以下载结果
- [ ] 飞书配置缺失时仍能正常运行

### F. 不翻车

- [ ] 聚类异常自动 fallback
- [ ] Embedding 缺失自动 fallback
- [ ] 时间字段异常不阻断主流程
- [ ] 任意非核心模块异常不导致整页崩溃

---

## 十四、Agent 执行顺序（必须遵守）

### Phase 1：架构确认

1. 读取本文件
2. 读取当前仓库结构
3. 输出“E01 架构对齐”
4. 明确当前项目是否从零开始或在旧代码上修改
5. 不改变已经确定的技术栈

### Phase 2：先跑通最小闭环

按顺序实现：

```text
loader
→ normalizer
→ entity_extractor
→ cluster
→ classifier
→ main
```

先确保：

> 上传 CSV → 得到多频结果 → 页面展示

### Phase 3：补业务价值层

在最小闭环通过后实现：

```text
event_profiler
→ risk_analyzer
→ action_advisor
```

让：

> 多频事件 → 事件画像 → 风险 → 建议

完整跑起来。

### Phase 4：增强输出

最后再增加：

```text
趋势图
区域分布
Excel
飞书
```

### Phase 5：真实数据适配

主办方数据到位后：

1. 先检查字段
2. 检查“多频”业务定义
3. 检查真实数据的时间跨度
4. 调整词典
5. 调整聚类参数
6. 调整风险规则

**不得因为真实数据变化就推翻整体架构。**

---

## 十五、Agent 禁止事项

1. 不得自行改成 FastAPI，除非用户明确要求
2. 不得默认调用外部大模型 API
3. 不得运行时下载 Embedding 模型
4. 不得为了展示 AI 而增加复杂 Agent 框架
5. 不得把“频次高”直接等价成“高风险”
6. 不得伪造准确率、提升比例、节省时间等业务数字
7. 不得伪造无法从数据得出的趋势
8. 不得伪造部门归属
9. 不得因为聚类效果一般就删除异常数据
10. 不得为了视觉效果牺牲核心业务结果的可解释性
11. 不得将真实密钥、Webhook、Token 写死在代码中
12. 不得在用户没有要求时大面积重构仓库

---

## 十六、Agent 完工时必须汇报

完成后必须一次性回答：

```text
【E01 实施完成】
1. 已完成哪些模块
2. 当前实际使用的技术路线
3. Demo 如何启动
4. 测试数据是什么
5. Demo 已跑通到哪一步
6. 当前聚类是否走 TF-IDF / Embedding
7. 是否触发过 fallback
8. 当前高频事件能否输出风险等级
9. 当前高频事件能否输出处置建议
10. 哪些能力仍为可选项
11. 当前已知问题
12. 下一步最值得优化的 3 项
```

---

## 十七、最终产品定位

本项目对外不要只描述成：

> “12345 多频工单识别系统。”

推荐定位：

> **12345 高频事件智能预警与处置辅助系统**

一句话解释：

> **通过 AI 对海量 12345 工单进行自动聚类和事件画像，及时发现正在形成的高频问题，识别趋势与重点区域，并为管理人员提供风险优先级和处置建议。**

---

## 十八、最终原则

```text
不是为了聚类而聚类
不是为了 AI 而 AI
不是为了看板而看板

而是：

从海量工单里
发现正在形成的问题
↓
判断哪些问题值得优先关注
↓
告诉管理人员为什么
↓
给出下一步建议
↓
必要时进入工作流
```

**最终验收标准不是“代码写完了”，而是：一个评委在不看源码的情况下，能够在 1 分钟内理解系统发现了什么问题、为什么值得关注、下一步应该做什么。**
