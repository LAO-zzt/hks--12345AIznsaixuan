# 12345工单清洗模块配置文件（模板，可安全入库）
# 使用方法：复制本文件为 config.yaml 并根据实际情况修改；config.yaml 已被 gitignore，不会入库。

# ============ LLM 配置 ============
llm:
  # 模式选择: ollama (本地) / openai (云端)
  mode: openai

  # Ollama 本地配置
  ollama:
    api_url: http://localhost:11434/api/chat
    model: qwen2.5:7b

  # OpenAI 兼容接口配置（支持各种国内大模型）
  openai:
    api_url: https://api.deepseek.com
    api_key: ""  # 占位符：真实 Key 请从环境变量注入，切勿提交到仓库
    model: deepseek-v4-flash

  # 通用参数
  temperature: 0.1
  max_tokens: 500
  timeout: 30  # 请求超时时间（秒）

# ============ 高德地图配置 ============
gaode:
  # 高德地图 API Key（用于POI验证）
  # 申请地址: https://console.amap.com/dev/key/app
  api_key: ""  # 占位符：真实 Key 请从环境变量注入，切勿提交到仓库

  # POI缓存文件路径
  cache_file: data/cache/shunde_poi_cache.json

  # 是否启用高德验证（false则只用LLM）
  # 注意：全量分析约12.8万条，开启会触发高德API限流，调优阶段再开启
  enabled: false

# ============ 数据库配置 ============
database:
  db_path: data/cleaner.db
  batch_size: 200

# ============ 清洗参数配置 ============
cleaning:
  min_quality_score: 0.4
  max_consecutive_batch_failures: 3
  dedup_similarity_threshold: 0.85

# ============ 数据源配置 ============
source:
  default_excel: data/testdata/政数局资料-顺德区12345热线工单（2025年1月至3月）.xlsx
  upload_dir: data/uploads

# ============ Web服务配置 ============
web:
  port: 5000
  debug: false
  max_upload_size_mb: 500

# ============ 日志配置 ============
logging:
  level: INFO
  log_file: data/logs/cleaner.log
